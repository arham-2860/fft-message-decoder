# FFT Message Decoder (MATLAB)

This script extracts a hidden message from a stego image using 2D FFT.

## Requirements
- MATLAB
- Stego image (.png/.jpg/.bmp)
- stego_info.mat in same directory

## Run
Run `decode_message.m` and select the stego image.
